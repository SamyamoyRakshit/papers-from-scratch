.
#
# Reuse legend (relative to ../transformer/):
#   [import] NOT a local file — imported directly from transformer/ (zero changes, single source of truth)
#   [tweak]  copied here with one small change
#   [adapt]  reuse the pattern, rewrite the body for BERT
#   [new]    BERT-specific, written from scratch
#
# Imported from ../transformer/ (apple-to-apple, no change — run from repo root so
# `transformer` resolves as a package; all three import only torch, no relative deps):
#   transformer/models/modules/multi_head_attention.py  → MultiHeadAttention
#   transformer/models/modules/layer_norm.py            → LayerNorm
#   transformer/utils/mask_utils.py                     → create_padding_mask (encoder needs only the pad mask)
# Shared across replications (repo root): common/run_utils.py — device, seed, git hash,
# sha256, logging, leaderboard helpers.
#
├── README.md                        # Results + docs map + setup + full usage runbook
├── repo.txt                         # This annotated file tree
├── requirements.txt                 # pip fallback (pyproject.toml at repo root is the source of truth)
├── models/
│   ├── __init__.py
│   ├── bert.py                      # [new]   full model: embeddings + encoder + pooler ([CLS])
│   ├── encoder.py                   # [tweak] copy of transformer's; only change = FFN import line → BERT's GELU feed_forward (MHA+LayerNorm still imported from transformer)
│   ├── heads.py                     # [new]   MLM head (weight-tied to token emb) + NSP head
│   ├── bert_for_pretraining.py      # [new]   pre-train assembly: BERTModel + BERTPreTrainingHeads (tie + head-init wired here)
│   ├── bert_for_classification.py   # [new]   fine-tune assembly: body + dropout + Linear(H, K) on pooled [CLS] (§4.1)
│   └── modules/
│       ├── __init__.py
│       ├── embeddings.py            # [new]   token + SEGMENT + LEARNED position, summed + LayerNorm + dropout (NO √d_model)
│       └── feed_forward.py          # [tweak] copy of transformer's; swap ReLU → tanh-GELU
│       # MultiHeadAttention, LayerNorm → [import] from ../transformer (see header)
│       # positional_encoding NOT used — BERT learns position emb inside embeddings.py
├── utils/
│   ├── __init__.py
│   ├── config.py                    # [adapt] strict pydantic YAML config (pre-training), repo-root-relative paths
│   ├── masking.py                   # [new]   MLM masking: 15% of tokens → 80% [MASK] / 10% random / 10% keep
│   ├── nsp.py                       # [new]   NSP pair builder: 50% true next-sentence, 50% random sentence B
│   ├── loss.py                      # [adapt] reuse F.cross_entropy approach; MLM CE (masked only, ignore_index) + NSP CE
│   ├── optimizer.py                 # [adapt] same builder shape; AdamW + linear warmup→linear decay (not Noam)
│   ├── data_utils.py                # [adapt] tokenizer+batching pattern; new: WordPiece + [CLS]/[SEP] + MLM/NSP examples
│   ├── train_utils.py               # [adapt] pre-training loop + checkpointing; handles the two losses
│   ├── finetune_config.py           # [new]   typed FinetuneConfig: pretrained checkpoint+snapshot → downstream dataset
│   ├── finetune_data.py             # [new]   sna.bn → [CLS] text [SEP] ClassificationDataset → dynamic-pad loaders (+ per-class label_counts)
│   └── finetune_utils.py            # [new]   fine-tune train/validate loop; best-by-val-acc + leaderboard + symlink
├── scripts/
│   ├── prepare_corpus.py            # [new]   run-once: Bengali Wikipedia → cleaned corpus .txt (one article = one block)
│   ├── _corpus_filter.py            # [new]   optional per-article keep-hook for prepare_corpus — gitignored, falls back to keep-all
│   ├── _common.py                   # [adapt] shared checkpoint load helpers (pretrain model build + weights_only load)
│   ├── pretrain.py                  # [adapt] from train.py — PRE-TRAINING entrypoint: MLM + NSP on the unlabeled corpus, --resume support
│   ├── finetune.py                  # [new]   FINE-TUNING: transplant bert.* body, fresh classifier head, train on sna.bn (optional class-weighted CE via config)
│   ├── evaluate.py                  # [adapt] held-out TEST metrics: accuracy + per-class P/R/F1 + confusion matrix
│   ├── inference.py                 # [new]   one raw Bengali sentence → predicted topic + full softmax; owns load_finetuned_classifier() (shared with app)
│   └── app.py                       # [new]   local Gradio demo — loads once at startup, gr.Label probability bars
├── configs/
│   ├── base.yaml                    # pre-training recipe: paper BERT-base scaled for M1 16GB (L=6, H=256, A=8, d_ff=1024)
│   ├── tiny.yaml                    # small model (L=2, H=64) for quick pipeline sanity checks
│   └── finetune.yaml                # fine-tuning: §A.3 hyperparams (3 epochs, batch 32, lr sweep) + class_weighting flag
├── data/                            # corpus .txt files from prepare_corpus — runtime, gitignored
├── tokenizer/                       # trained WordPiece vocab (base/, tiny/) — runtime, gitignored
├── checkpoints/                     # run_<ts>/ dirs (best.pt, last.pt, config.yaml snapshot) + leaderboard.json + best.pt symlink — runtime, gitignored
├── logs/                            # TensorBoard event files (base/, finetune/) — runtime, gitignored
└── docs/                            # standardized docs (Diátaxis; the "why" lives on the blog)
    ├── getting-started.md           # install + verify the pipeline on the tiny config
    ├── training.md                  # reproduce 86.5%: pretrain → finetune → evaluate → serve
    └── architecture.md              # model spec, config field reference, checkpoint layout
