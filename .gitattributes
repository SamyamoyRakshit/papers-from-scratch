# Keep GitHub's language bar accurate: don't count notebooks (their embedded
# outputs dwarf the real Python). Shows the repo as Python, not Jupyter Notebook.
# detectable=false removes them from stats but keeps notebook diffs visible.
*.ipynb linguist-detectable=false
